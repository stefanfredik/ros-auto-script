# Membuat virtual ENV Python

```bash
    python -m venv [my env]
```

# Mengaktifkan Virtual Env

```bash
    source /bin/activate
```

# Install Module

```bash
    pip install -r requirements.txt
```